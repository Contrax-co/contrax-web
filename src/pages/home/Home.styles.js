import styled from 'styled-components/macro';

export const StyledCtaBar = styled('div')(
  (props) => ({
    background: '#000',
    padding: '1rem',
    border: '1px solid',
    borderRadius: '1rem',
    marginBottom: '2rem',
  })
);